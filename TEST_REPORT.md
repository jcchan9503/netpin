# NetPin 验证记录（2026-09-14，构建验证中）

本地已实际执行：静态检查、41 个单元/回归测试、4 个真实 UDP SNMP 回环测试、Electron 界面表单交互检查、Linux 打包目录启动检查。

当前容器不具备 ICMP socket 权限；直接执行系统 ping 返回 Operation not permitted，因此本地 ICMP 集成测试显式跳过，不计入通过项。GitHub CI 会在独立 Windows/Linux 运行器上继续执行该测试。

本地 Electron 44.3.0 已完成：新增凭据、凭据脱敏、新增设备、重复设备拒绝、新建网段、人工地址预留、备注 HTML 转义、分页、CSV 后端导出、SQLite 读写和四个页面截图。根权限容器的这次测试加了 --no-sandbox，仅限隔离测试；产品默认仍开启 sandbox。

已修复的回归问题：共享 FDB 定位提示、未知 ARP 类型、未来时间戳、并发探测失败后排空，以及凭据保存的异步竞态。修复了桌面测试自身重置示例后状态未刷新的问题，截图现在会等待并验证正确页面。

远端 Windows EXE、Linux x64/ARM64 安装包和安装后启动结果，将在 CI 结束后补充。没有真实支行设备、Windows 10 真机或指定 UOS/麒麟镜像的验收；不将模拟设备或通用 CI 系统说成已完成生产验证。
