# NetPin 0.1.0 验证与交付记录

日期：2026-09-14。

**结论：本次代码补齐、回归修复、三平台构建、EXE/DEB 安装及安装后 Electron 功能检查已完成。未将缺少真机条件的项目计入通过项。**

## 可复核的版本与流水线

- 完整验证的应用代码提交：`5eb9e0d9c6e646627afea7792899aa133127ca0d`。
- 最终成功流水线：[NetPin build and test #4](https://github.com/jcchan9503/netpin/actions/runs/34832253734)。
- 该提交之后的收尾修改仅更新本报告及安装说明，没有改变应用代码和安装器二进制。
- 变更通过 [PR #2](https://github.com/jcchan9503/netpin/pull/2) 交付；是否合并请以 PR 实际状态为准。
- 源码、锁文件、测试及 Windows/Linux 构建和安装脚本在仓库中；安装器与原始日志/截图作为该流水线的 artifacts 保存，保留期为 7 天。源码不含 node_modules、运行时、真实网络台账或凭据。

## 实际执行结果

以下是同一套测试分别在三个原生平台执行的结果，不是将平台重复次数当作不同测试用例。

| 实际运行环境 | 单元/回归 | 回环集成 | 源码启动界面检查 | 安装后界面检查 |
| --- | --- | --- | --- | --- |
| Windows Server 2022 x64 | 41 通过 | 5 通过、0 跳过 | 通过 | EXE 安装后通过 |
| Ubuntu 22.04 x64 | 41 通过 | 5 通过、0 跳过 | 通过 | DEB 安装后通过 |
| Ubuntu 22.04 ARM64 | 41 通过 | 5 通过、0 跳过 | 通过 | DEB 安装后通过 |

三个平台的测试失败数均为 0。静态语法、必需文件、锁文件一致性和桌面安全配置检查也均通过。

### 集成测试内容

1. 真实 UDP 回环 SNMPv2c：接口、ARP、桥端口/ifIndex 与 VLAN/FDB 表读取。
2. 真实 UDP 回环 SNMPv1：旧版 ARP 表回退与相关表读取。
3. 真实 UDP 回环 SNMPv3：SHA256/AES authPriv 认证加密采集。
4. 错误凭据请求在取消后终止。
5. 系统 ICMP 向 `127.0.0.1` 发送 Echo 并获得响应；Windows 使用已编译的独立 C# 辅助程序。

测试 SNMP Agent 在本机监听，仅使用合成表项；这是真实协议交互，不是物理交换机或支行网络的验收。

### Electron 实际界面检查

源码启动和安装后的程序均经过界面控件操作，覆盖新增凭据、新增设备、重复设备拒绝、新建网段、地址预留、SQLite 写入与读取、备注 HTML 转义、分页及页面导航。检查了渲染进程没有 Node.js 全局对象、凭据不出现在普通状态响应，以及视口横向溢出。

导出验证覆盖 CSV 后端生成和内容，不包含对各系统原生“另存为”对话框的自动点击测试。示例数据中 `192.0.2.21` 关联到 `Gi0/1` 的结果经过断言。

三个平台都输出了安装后 `electron-smoke.json` 和网络概览、IP 台账、设备与端口、凭据设置四个页面的真实截图。最终安装后测试的 `sandboxDisabledForRootTest` 均为 `false`，即没有通过关闭沙箱换取运行成功。

## 本次修复

- 共享 FDB 对应多个 VLAN 时保留候选定位的不确定性。
- 未知 ARP 类型不再当成动态发现证据，仅 type=3 按动态处理。
- 未来时间戳不再被判断为新鲜探测记录。
- 并发探测异常或取消后等待所有任务结束，避免后台残留请求。
- 异步凭据加密返回后重新校验采集锁，避免并发修改。
- Windows Framework C# 编译器使用原生绝对路径，修复辅助程序构建失败。
- 最小 Windows 内容视口的横向滚动条问题，并加入布局回归断言。
- Down 接口不再错误显示“尚未采集”；界面定期更新证据时效。
- 修复测试自身重置示例后状态未刷新的问题，截图前等待正确页面完成加载。

## 已生成的安装器

| 文件 | 字节数 | 安装验证 |
| --- | ---: | --- |
| `NetPin-0.1.0-win10-x64-setup.exe` | 114548389 | Windows Server 2022 x64 已安装并运行 |
| `NetPin-0.1.0-linux-amd64.deb` | 99350852 | Ubuntu 22.04 x64 已安装并运行 |
| `NetPin-0.1.0-linux-arm64.deb` | 94414416 | Ubuntu 22.04 ARM64 已安装并运行 |
| `NetPin-0.1.0-linux-x86_64.rpm` | 88667665 | 已构建；未进行 RPM 发行版安装测试 |
| `NetPin-0.1.0-linux-aarch64.rpm` | 84375521 | 已构建；未进行 RPM 发行版安装测试 |
| `NetPin-0.1.0-linux-x64.tar.gz` | 119143669 | 已构建的目录分发包 |
| `NetPin-0.1.0-linux-arm64.tar.gz` | 120137386 | 已构建的目录分发包 |

下载回本次工作环境后，以上七个二进制文件均按同包 `SHA256SUMS` 再次核验成功。收尾更新了外层安装说明中的架构文件名；二进制及其 SHA256 未改变。SHA256 是完整性校验，不是发布者代码签名。

## 跳过、未验证与不支持的项目

**环境限制，已跳过：** 本地开发容器缺少 ICMP socket 权限，系统 ping 返回 Operation not permitted，因此本地此项显式跳过；已在上述三个远端运行器补做并通过。本地独立浏览器访问 HTTP 预览受环境策略阻止，因此没有将该预览认定为通过；实际 Electron 桌面已验证。根权限开发容器曾仅为隔离测试添加 --no-sandbox，最终交付和远端安装测试未使用该参数。

**缺少目标环境，未验证：** 没有用户支行网络访问条件、实际锐捷/迈普设备及固件、Windows 10 真机、指定统信 UOS/银河麒麟镜像。不能据通用 CI 系统通过就宣称这些目标已验收。厂商私有 MIB、特定 VLAN context/VRF、聚合端口和实际 description 字段仍需试点核实。没有进行全行规模性能、安全渗透、长期稳定性或升级/卸载全流程验收。

**当前不支持：** LoongArch/MIPS/SW64 运行时、IPv6 地址台账、DHCP/DNS 服务器管理、完整拓扑与自动下级交换机追踪、厂商 SSH 兜底、常驻系统服务和自动定时扫描。本版按用户点击执行设备采集和网段扫描，不提供 SNMP SET 或设备配置下发。

**未签名：** 未提供商业代码签名证书，不绕过单位应用白名单或终端安全审批。不得将本版描述为经过银行生产或信创认证。

## 建议的现场验收起点

先在获授权的测试电脑安装，录入一个支行的一台实际网关和一台接入交换机，校验已知终端的 IP、MAC、接口索引和 description。再测试已分配但关机的地址、上联口多 MAC、采集超时等场景。未发现使用证据不等于可分配，任何扫描都不能自动释放人工分配台账。
